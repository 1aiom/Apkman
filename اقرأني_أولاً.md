# حاسبة بتصميم الآيفون - مشروع Android Studio كامل

## الخطوات (3 خطوات بسيطة):

### 1) افتح Android Studio على جهازك
- اختر **New Project** → **Empty Views Activity**
- اسم المشروع: `Calculator` (أو أي اسم)
- Language: **Kotlin**
- Minimum SDK: **API 21** أو أعلى
- اضغط **Finish** وخلي المشروع يخلص التحميل (Gradle Sync)

### 2) استبدل الملفات
افتح المشروع الجديد اللي انسوى، واستبدل هذي الملفات بالملفات اللي بهذا الفولدر:

| الملف عندك (الافتراضي) | استبدله بـ |
|---|---|
| `app/src/main/AndroidManifest.xml` | `AndroidManifest.xml` |
| `app/src/main/java/.../MainActivity.kt` | `MainActivity.kt` |
| `app/src/main/res/layout/activity_main.xml` | `activity_main.xml` |
| `app/src/main/res/values/themes.xml` | `themes.xml` |
| `app/build.gradle` (أو `app/build.gradle.kts`) | `build.gradle` |

وأضف هذي الملفات الجديدة (مو موجودة بالمشروع الافتراضي، سويها يدوي):
- `app/src/main/res/values/styles.xml`
- `app/src/main/res/drawable/btn_num_bg.xml`
- `app/src/main/res/drawable/btn_op_bg.xml`
- `app/src/main/res/drawable/btn_func_bg.xml`

**مهم:** لازم `package name` بملف MainActivity.kt (أول سطر `package com.user.calculator`) يطابق اسم الفولدر اللي مسوّيه Android Studio تلقائياً. إذا اسم الباكج عندك مختلف، غيّر السطر الأول بـ MainActivity.kt ليطابق اسم مشروعك، أو انقل الملف لنفس المسار.

### 3) ابني التطبيق
- من فوق: **Build → Build Bundle(s)/APK(s) → Build APK(s)**
- بعد ما يخلص، اضغط على **locate** بالإشعار اللي يطلع تحت، بتلقى ملف:
  `app/build/outputs/apk/debug/app-debug.apk`
- انسخه للسامسونك (عن طريق كيبل، بلوتوث، أو تيليجرام) وثبته — راح يطلب منك تفعيل "تثبيت من مصادر غير معروفة" أول مرة، وافق عليها.
- أرسل نفس ملف الـ APK لجماعتك مباشرة عبر واتساب/تيليجرام وهم يثبتونه بنفس الطريقة.

---

## المميزات
- تصميم مطابق لحاسبة الآيفون (خلفية سوداء، أزرار دائرية، برتقالي للعمليات)
- عمليات: جمع، طرح، ضرب، قسمة، نسبة مئوية %، تغيير الإشارة +/-
- يشتغل بدون إنترنت 100%، ما يحتاج أي صلاحيات
- مجاني بالكامل، الكود كله ملكك، بدون أي حقوق أو قيود

## ملاحظة
هذا مشروع Kotlin حقيقي (مو HTML)، لازم يتبني (compile) داخل Android Studio عشتصير APK حقيقية تشتغل native على النظام. ما فيه طريقة تسوي APK بدون Android Studio أو أداة بناء مشابهة.
